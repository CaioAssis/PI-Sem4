//Descrição dos módulos a serem preenchidos na inspeção

import { Router } from 'express'
import authMiddleware from '../../middlewares/auth.middleware'

const moduloDescRoutes = Router()

export default moduloDescRoutes